(function ($) {
  Drupal.behaviors.OwlCarousel = {
    attach: function(context,settings){
       // unsetting the function from SPruit
    }
  };
  Drupal.behaviors.SiteWaypoints = {
    attach: function(context,settings) {
    }
  };  
})(jQuery);
